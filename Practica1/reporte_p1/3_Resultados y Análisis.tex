\textcolor{black}{\section{Resultados y Análisis}}
Al graficar los datos de corriente y voltaje del diodo en polarización directa se obtiene la grafica que se muestra en figura (\ref{PD}), con su respectiva incertidumbre asociada a cada valor. Donde podemos observar que para voltajes $V_d$ inferiores a aproximadamente 0.4V la $I_d \approx 0 $ mientras que para ($V_d<0.4V$) la corriente empresa a crecer de manera exponencial. Mostrando el comportamiento característicos de la unión de materias tipo p-n.
\begin{figure}[htbp]
	\centering
	\includegraphics[scale=0.35]{figs/PD.png}
	\caption{Polarización directa }
	\label{PD}
\end{figure}

Un vez con estos datos utilizamos la ecuación (\ref{shock3}) para obtener la relación $e/k_n$ obteniendo el ajuste lineal graficando $In(I_d)$ en función de $V_d$ como muestra la figura (\ref{log-log})
\begin{figure}[htbp]
	\centering
	\includegraphics[scale=0.35]{figs/log-log.png}
	\caption{Ajuste lineal en polarización directa  }
	\label{log-log}
\end{figure}
\noindent 